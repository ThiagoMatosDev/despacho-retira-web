import { Routes } from '@angular/router';
import { DispatcherComponent } from './dispatcher/dispatcher.component'; // Ajuste o caminho, se necessário

export const routes: Routes = [
  { path: '', component: DispatcherComponent }, // Rota inicial
];
